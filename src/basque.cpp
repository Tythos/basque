/**
 * @author "Brian Kirkpatrick" <code@tythos.net>
 */

#include "basque.h"

float getSquare(float x) {
    return x * x;
}

float getCube(float x) {
    return x * x * x;
}

void printNumber(float x) {
    std::cout << "Oh, hey there, it's the number " << x << "!" << std::endl;
}
